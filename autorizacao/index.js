indexes: [
    {
        unique: true,
        fields: ['user_id', 'count', 'name']
    }
]

indexes: [{ unique: true, fields: ['someUnique'] }]

consttraint: 
{

    unique: 
    {
        [ 'cfopId', 'csosnId', 'icmId']
    }
}